# Overview

EVM Escrow Suite is a modular escrow layer for programmable conditional payments.

It is designed for products where money should only move when predefined rules are satisfied, such as dual approval, timeout, or arbiter resolution.
